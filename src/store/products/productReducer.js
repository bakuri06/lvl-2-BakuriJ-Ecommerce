const initialState = {
    isLoggingIn: false,
    products: [],
    token: "",
  };
  
  export default function productsReducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state;
    }
  }
  